/**
 * Generates a random integer between 0 and 50 (inclusive).
 * @returns {number} A random integer between 0 and 50.
 */
export const getRandomNum = () => {
  return Math.floor(Math.random() * 51);
}